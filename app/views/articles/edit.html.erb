<h1>Editing article</h1>

<%= form_with model: @article do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :content %>
  <%= f.text_area :content, data_editor: 'markdown' %>
  <%# want to show html info here not content, added data-editor="markdown" like this ^ %>
  <%# still not working though... tags show up in content once saved %>
  <%= f.submit %>
<% end %>

<%= link_to 'Show', article_path(@article) %> |
<%= link_to 'Back', articles_path %>
